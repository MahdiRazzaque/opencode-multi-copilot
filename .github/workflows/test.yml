name: Test

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  test:
    uses: ./.github/workflows/bun-command.yml
    with:
      job_name: Test
      run_command: bun run test
