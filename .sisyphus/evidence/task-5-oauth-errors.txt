Task 5 evidence: OAuth slow_down and failure handling

Verification command:
- `bun test src/auth.test.ts`

Observed result:
- 7 pass, 0 fail

Covered assertions:
- `slow_down` response increases wait to `sleep(10000)` for interval 2 + 5 seconds + safety margin
- Unrecoverable OAuth error returns `{ type: "failed" }`
- Polling stays inside `callback()` rather than `authorize()`
