Task 5 evidence: OAuth authorise shape

Verification command:
- `bun test src/auth.test.ts`

Observed result:
- 7 pass, 0 fail

Covered assertions:
- `createAuthHook()` returns `{ provider: "multi-copilot", loader, methods }`
- `authorize()` returns `{ url, instructions, method: "auto", callback }`
- Device code request posts to `https://github.com/login/device/code`
- Request body includes `client_id: "Ov23li8tweQw6odWQebz"`
- Instructions string is `Enter code: ABCD-1234`

Supporting repo checks:
- `bun run typecheck` passed
- `bun run build` passed
